import { Checkbox  } from "@mui/material";


const Checkbox = ({ hostInfo, setHostInfo }) => {
    return (
        <FormControlLabel
        label="I accept whatever happens during the game im totally responsible for the causes. lorem ipsum"
        control={<Checkbox  />}
      />
    )
}